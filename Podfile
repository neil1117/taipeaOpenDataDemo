# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'TaipeiOpenDataDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TaipeiOpenDataDemo
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'RxDataSources'
  pod 'SDWebImage'

  target 'TaipeiOpenDataDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TaipeiOpenDataDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
